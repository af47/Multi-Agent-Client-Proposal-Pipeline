# Anthropic Claude SDK
anthropic>=0.40.0

# Pydantic for typed schemas and validation
pydantic>=2.6.0

# Environment variable loading
python-dotenv>=1.0.0

# Standard library only — no additional dependencies needed for:
# - logging (stdlib)
# - json (stdlib)
# - pathlib (stdlib)
# - dataclasses (stdlib)
# - argparse (stdlib)
# - hashlib (stdlib)
# - time (stdlib)
# - uuid (stdlib)
